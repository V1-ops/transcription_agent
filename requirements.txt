# ── AI Meeting Assistant ── requirements.txt ──────────────────────────────────
# Python >= 3.10 recommended

# ── Audio / Video acquisition ─────────────────────────────────────────────────
yt-dlp>=2026.7.4                  # Download audio from YouTube URLs
pydub>=0.25.1                     # Audio conversion and chunking (requires FFmpeg)

# ── Speech-to-Text (Hugging Face Inference + Sarvam fallback) ────────────────
# English   → Hugging Face Inference API (HF_TOKEN)
# Hinglish  → Sarvam (SARVAM_API_KEY, signup credits)
huggingface_hub>=0.36.2           # InferenceClient for hosted ASR

# ── LLM Orchestration — LangChain LCEL + Mistral ──────────────────────────────
langchain>=0.2.0
langchain-core>=0.2.0
langchain-community>=0.2.0
langchain-mistralai>=1.1.5        # Mistral LLM + embeddings API
mistralai>=0.4.0                  # Official Mistral Python client

# ── RAG Pipeline ──────────────────────────────────────────────────────────────
chromadb>=0.5.0                   # Local vector store
langchain-chroma>=0.1.2           # Chroma integration used by core/vectore_store.py
tiktoken>=0.7.0                   # Token counting for text splitting
fastembed>=0.4.0                  # ONNX embeddings (replaces torch-heavy sentence-transformers)

# ── Streamlit UI ──────────────────────────────────────────────────────────────
streamlit>=1.35.0
streamlit-extras>=0.4.0           # Additional Streamlit widgets / utilities

# ── PDF & TXT Export ──────────────────────────────────────────────────────────
reportlab>=4.2.0                  # PDF generation
fpdf2>=2.7.9                      # Lightweight alternative PDF library

# ── Utilities ─────────────────────────────────────────────────────────────────
python-dotenv>=1.0.0              # Load API keys from .env
numpy>=1.26.0
tqdm>=4.66.0                      # Progress bars
requests>=2.32.0
